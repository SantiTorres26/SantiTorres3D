<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Proyecto</title>
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <div class="admin-panel">
    <h1>Editar Proyecto</h1>
    <form action="/admin/projects/edit/{{project.id}}" method="POST" class="admin-form">
      <input type="text" name="title" placeholder="Título del proyecto"
             value="{{project.title}}" required>
      <textarea name="description" placeholder="Descripción"
                rows="4" required>{{project.description}}</textarea>
      <input type="date" name="date" value="{{project.date}}" required>
      <button type="submit">Guardar Cambios</button>
    </form>
    <a href="/admin/projects" class="btn">Cancelar</a>
  </div>
</body>
</html>
